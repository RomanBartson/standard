<header>Form</header>
<section>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input
        type="submit" value="Create"
        class="btn btn-default pull-right"
    />
    {{ form_end(form) }}
</section>